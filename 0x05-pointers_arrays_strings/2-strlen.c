#include "main.h"
#include <string.h>

/**
 * _strlen: will return the length of a string
 *
 * @s: the input of s is a char
 *
 * Return: Always 0 for success
*/
int _strlen(char *s)
{
	return (strlen(s));
}
